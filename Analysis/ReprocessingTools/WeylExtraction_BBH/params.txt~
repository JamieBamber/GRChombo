### parameters file for Flux extraction

#Params for runtime inputs
N1 = NBASIC
N2 = NBASIC
N3 = NSPACE3
L = LSPACE
max_level = 3 # There are (max_level+1) grids, so min is zero
dt_multiplier = DTMULT

# For read in of files
data_rootdir =/p/project/pra116/bamber1/BinaryBHScalarField/
data_subdir = SUBDIR
output_rootdir = /p/home/jusers/bamber1/juwels/GRChombo/Analysis/data/GR_Binary_BH_data/

# Files parameters
chk_prefix = BinaryBHSFChk_
plot_prefix = BinaryBHSFPlot_
plot_interval = PLOTINTERVAL
dt_multiplier = DTMULT
start_number = SNUMBER
end_number = ENUMBER
suffix = SUFFIX
# resume = RESUMEYN

# integration parameters
num_points_phi = NPHI
num_points_theta = NTHETA
extraction_center = CENTERX CENTERY CENTERZ
center = CENTERX CENTERY CENTERZ
extraction_radius = REXTRACT
extraction_level = 0
num_modes = 3
modes = 2 0
        2 1
	2 2
num_points_phi = NPHI
num_points_theta = NTHETA

# if not periodic, then specify the boundary type
# 0 = static, 1 = sommerfeld, 2 = reflective, 3 = extrapolating
# 4 = mixed, 5 = fudge
# (see BoundaryConditions.hpp for details)
hi_boundary = 4 4 4
lo_boundary = 4 4 2

# if reflective boundaries selected, must select
# parity values (in order given by UserVariables.hpp)
# 0 = even
# 1,2,3 = odd x, y, z
# 4,5,6 = odd xy, yz, xz
vars_parity            = 0 0 0 0 0 0 0 0 # chi phi Pi rho rhoJ J_R J_azimuth_R
vars_parity_diagnostic = 0 0 0 0    #

# if mixed boundaries selected, must specify which are extrapolating
# (the rest are assumed to be all sommerfeld)
num_extrapolating_vars = 2
extrapolating_vars = phi Pi
extrapolation_order = 0

# if sommerfeld boundaries selected, must specify any nonzero
# asymptotic values
num_nonzero_asymptotic_vars = 1
nonzero_asymptotic_vars = chi 
nonzero_asymptotic_values = 1.0

# From here it is mostly dummy params
verbosity = 1
nan_check = 1
regrid_threshold = 0.05
max_level = 9
regrid_interval = 0 0 0 128 64 64 64 32 16 0
max_grid_size = BOXSIZE
block_factor = BOXSIZE
isPeriodic = 0 0 0 
